---
layout: layout.liquid
---

{% for post in collections.post reversed %}
  <hr />  
  {% comment %} <a href="{{ post.url }}">{{ post.data.title }}</a> {% endcomment %}
  <p>{{ post.data.date | myDateString }}</p>
  <p>{{ post.templateContent }}</p>
  <br />
{% endfor %}
